<Application x:Class="OGRALAB.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OGRALAB.Helpers"
             Startup="Application_Startup">
    <Application.Resources>
        <!-- Value Converters -->
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <local:LoadingTextConverter x:Key="LoadingTextConverter"/>
        <local:RoleToVisibilityConverter x:Key="RoleToVisibilityConverter"/>
        <local:RoleToBooleanConverter x:Key="RoleToBooleanConverter"/>
        <local:BoolToStringConverter x:Key="BoolToStringConverter"/>
        <local:UserFormTitleConverter x:Key="UserFormTitleConverter"/>
        <local:PasswordLabelConverter x:Key="PasswordLabelConverter"/>
        <local:NavigationItemConverter x:Key="NavigationItemConverter"/>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <local:RadioButtonConverter x:Key="RadioButtonConverter"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <local:InverseNullToVisibilityConverter x:Key="InverseNullToVisibilityConverter"/>
        <local:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <local:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <local:StatusToTextConverter x:Key="StatusToTextConverter"/>
        <local:BoolToTextConverter x:Key="BoolToTextConverter"/>
        
        <!-- Global Styles -->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <!-- Color Resources -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#2E7D32"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#4CAF50"/>
        <SolidColorBrush x:Key="AccentColor" Color="#81C784"/>
        <SolidColorBrush x:Key="ErrorColor" Color="#F44336"/>
        <SolidColorBrush x:Key="WarningColor" Color="#FF9800"/>
        <SolidColorBrush x:Key="InfoColor" Color="#2196F3"/>
        
    </Application.Resources>
</Application>
