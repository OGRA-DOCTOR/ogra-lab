<Window x:Class="OGRALAB.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OGRALAB.Helpers"
        Title="OGRA LAB - نظام إدارة المختبر الطبي" 
        Height="800" Width="1200"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        FontFamily="Cairo, Segoe UI">
    
    <Window.Resources>
        <!-- Color Resources -->
        <SolidColorBrush x:Key="PrimaryDark" Color="#1e3a8a"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#f59e0b"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#10b981"/>
        <SolidColorBrush x:Key="LightGray" Color="#f8fafc"/>
        <SolidColorBrush x:Key="MediumGray" Color="#64748b"/>
        <SolidColorBrush x:Key="DarkText" Color="#1e293b"/>
        <SolidColorBrush x:Key="CardBackground" Color="White"/>

        <!-- Navigation Button Style -->
        <Style x:Key="NavigationButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Margin="5,0">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource AccentOrange}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#d97706"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Active Navigation Button Style -->
        <Style x:Key="ActiveNavigationButton" TargetType="Button" BasedOn="{StaticResource NavigationButton}">
            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <!-- Header Button Style -->
        <Style x:Key="HeaderButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentOrange}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#d97706"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#b45309"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Logout Button Style -->
        <Style x:Key="LogoutButton" TargetType="Button" BasedOn="{StaticResource HeaderButton}">
            <Setter Property="Background" Value="#dc2626"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#b91c1c"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#991b1b"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Background="{StaticResource LightGray}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header Bar -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryDark}" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo and Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Ellipse Width="35" Height="35" Fill="{StaticResource AccentOrange}" Margin="0,0,12,0"/>
                    <StackPanel>
                        <TextBlock Text="OGRA LAB" FontSize="18" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="نظام إدارة المختبر الطبي" FontSize="11" Foreground="#cbd5e1"/>
                    </StackPanel>
                </StackPanel>

                <!-- User Info -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Background="{StaticResource AccentGreen}" CornerRadius="4" Padding="12,6" Margin="0,0,15,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👤" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding CurrentUserDisplayName}" FontWeight="SemiBold" Foreground="White"/>
                            <TextBlock Text=" (" FontSize="12" Foreground="#d1fae5" Margin="5,0,0,0"/>
                            <TextBlock Text="{Binding CurrentUserRole}" FontSize="12" Foreground="#d1fae5"/>
                            <TextBlock Text=")" FontSize="12" Foreground="#d1fae5"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Clock -->
                    <Border Background="{StaticResource AccentOrange}" CornerRadius="4" Padding="12,6">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🕐" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Name="ClockLabel" FontWeight="SemiBold" Foreground="White"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Header Actions -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="👥 إدارة المستخدمين" 
                           Style="{StaticResource HeaderButton}"
                           Command="{Binding ManageUsersCommand}"
                           Visibility="{Binding CanViewUserManagement, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    
                    <Button Content="📊 سجل الدخول" 
                           Style="{StaticResource HeaderButton}"
                           Command="{Binding ShowLoginLogsCommand}"
                           Visibility="{Binding CanViewLoginLogs, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>

                <!-- Logout Button -->
                <Button Grid.Column="3" 
                       Content="🚪 تسجيل الخروج" 
                       Style="{StaticResource LogoutButton}"
                       Command="{Binding LogoutCommand}"/>
            </Grid>
        </Border>
        
        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Navigation Side Panel -->
            <Border Grid.Column="0" Background="{StaticResource PrimaryDark}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15">
                        
                        <!-- Navigation Title -->
                        <TextBlock Text="القائمة الرئيسية" 
                                 FontSize="18" FontWeight="Bold" 
                                 Foreground="White" 
                                 Margin="10,15,0,20"/>
                        
                        <!-- Patient Management Section -->
                        <TextBlock Text="إدارة المرضى" 
                                 FontSize="14" FontWeight="SemiBold" 
                                 Foreground="{StaticResource AccentOrange}" 
                                 Margin="10,0,0,8"/>
                        
                        <Button Content="📋 إدارة المرضى" 
                               Command="{Binding NavigateCommand}"
                               CommandParameter="PatientManagement"
                               Visibility="{Binding CanAddPatients, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource NavigationButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentSection}" Value="PatientManagement">
                                            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        
                        <Button Content="🔍 بحث وتعديل" 
                               Command="{Binding NavigateCommand}"
                               CommandParameter="SearchEdit">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource NavigationButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentSection}" Value="SearchEdit">
                                            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        
                        <!-- Test Management Section -->
                        <TextBlock Text="إدارة التحاليل" 
                                 FontSize="14" FontWeight="SemiBold" 
                                 Foreground="{StaticResource AccentOrange}" 
                                 Margin="10,20,0,8"/>
                        
                        <Button Content="🧪 إدخال النتائج" 
                               Command="{Binding NavigateCommand}"
                               CommandParameter="ResultEntry"
                               Visibility="{Binding CanManageTests, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource NavigationButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentSection}" Value="ResultEntry">
                                            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        
                        <Button Content="📊 معاينة وطباعة النتائج" 
                               Command="{Binding NavigateCommand}"
                               CommandParameter="ReportView"
                               Visibility="{Binding CanManageTests, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource NavigationButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentSection}" Value="ReportView">
                                            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        
                        <!-- System Management Section -->
                        <TextBlock Text="إدارة النظام" 
                                 FontSize="14" FontWeight="SemiBold" 
                                 Foreground="{StaticResource AccentOrange}" 
                                 Margin="10,20,0,8"
                                 Visibility="{Binding CanViewReports, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        
                        <Button Content="⚙️ الإعدادات" 
                               Command="{Binding NavigateCommand}"
                               CommandParameter="Settings"
                               Visibility="{Binding CanViewReports, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource NavigationButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentSection}" Value="Settings">
                                            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        
                        <Button Content="🧪 البيانات التجريبية" 
                               Command="{Binding OpenTestDataManagementCommand}"
                               Visibility="{Binding CanViewAdminFeatures, Converter={StaticResource BoolToVisibilityConverter}}"
                               Style="{StaticResource NavigationButton}"
                               Margin="0,5,0,0"
                               ToolTip="إدارة البيانات التجريبية وإنشاء بيانات الاختبار"/>
                        
                        <Button Content="⚡ مراقب الأداء" 
                               Command="{Binding OpenPerformanceMonitorCommand}"
                               Visibility="{Binding CanViewAdminFeatures, Converter={StaticResource BoolToVisibilityConverter}}"
                               Style="{StaticResource NavigationButton}"
                               Margin="0,5,0,0"
                               ToolTip="مراقبة أداء النظام وتحسين الذاكرة"/>
                        
                        <!-- Statistics Section -->
                        <Border Background="#2563eb" 
                              CornerRadius="8" 
                              Padding="15" 
                              Margin="5,30,5,0">
                            <StackPanel>
                                <TextBlock Text="الإحصائيات اليومية" 
                                         FontSize="14" FontWeight="Bold" 
                                         Foreground="White" 
                                         Margin="0,0,0,12"/>
                                
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="إجمالي المرضى:" Foreground="#bfdbfe" FontSize="12"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TotalPatientsCount}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                    
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="التحاليل النشطة:" Foreground="#bfdbfe" FontSize="12" Margin="0,4,0,0"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ActiveTestsCount}" Foreground="White" FontWeight="Bold" FontSize="12" Margin="0,4,0,0"/>
                                    
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="التحاليل المعلقة:" Foreground="#bfdbfe" FontSize="12" Margin="0,4,0,0"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PendingTestsCount}" Foreground="White" FontWeight="Bold" FontSize="12" Margin="0,4,0,0"/>
                                    
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="مكتملة اليوم:" Foreground="#bfdbfe" FontSize="12" Margin="0,4,0,0"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding CompletedTodayCount}" Foreground="White" FontWeight="Bold" FontSize="12" Margin="0,4,0,0"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            
            <!-- Main Content Panel -->
            <Border Grid.Column="1" Background="{StaticResource CardBackground}" 
                   CornerRadius="8" Margin="15">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>
                
                <Grid>
                    <!-- Content Host -->
                    <ContentPresenter Name="MainContentPresenter" 
                                    Content="{Binding CurrentSectionContent}"/>
                    
                    <!-- Default Welcome Screen -->
                    <Grid Name="WelcomeScreen" 
                         Visibility="{Binding CurrentSectionContent, Converter={StaticResource NullToVisibilityConverter}}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="30">
                            <StackPanel>
                                <!-- Welcome Header -->
                                <StackPanel HorizontalAlignment="Center" Margin="0,40,0,50">
                                    <Ellipse Width="80" Height="80" Fill="{StaticResource AccentOrange}" Margin="0,0,0,20"/>
                                    <TextBlock Text="مرحباً بك في نظام OGRA LAB" 
                                             FontSize="28" FontWeight="Bold" 
                                             HorizontalAlignment="Center" 
                                             Foreground="{StaticResource DarkText}"
                                             Margin="0,0,0,10"/>
                                    
                                    <TextBlock Text="نظام إدارة المختبر الطبي المتطور" 
                                             FontSize="18" 
                                             HorizontalAlignment="Center" 
                                             Foreground="{StaticResource MediumGray}"/>
                                </StackPanel>
                                
                                <!-- Quick Stats -->
                                <Grid Margin="0,0,0,40">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0" Background="{StaticResource AccentGreen}" 
                                           CornerRadius="12" Padding="20" Margin="10">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="👥" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                            <TextBlock Text="المرضى" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="White" FontSize="14"/>
                                            <TextBlock Text="{Binding TotalPatientsCount}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Grid.Column="1" Background="{StaticResource AccentOrange}" 
                                           CornerRadius="12" Padding="20" Margin="10">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="🧪" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                            <TextBlock Text="التحاليل النشطة" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="White" FontSize="14"/>
                                            <TextBlock Text="{Binding ActiveTestsCount}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Grid.Column="2" Background="#f59e0b" 
                                           CornerRadius="12" Padding="20" Margin="10">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="⏳" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                            <TextBlock Text="المعلقة" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="White" FontSize="14"/>
                                            <TextBlock Text="{Binding PendingTestsCount}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Border Grid.Column="3" Background="{StaticResource PrimaryDark}" 
                                           CornerRadius="12" Padding="20" Margin="10">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="✅" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                            <TextBlock Text="مكتملة اليوم" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="White" FontSize="14"/>
                                            <TextBlock Text="{Binding CompletedTodayCount}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                                
                                <!-- Quick Actions -->
                                <StackPanel>
                                    <TextBlock Text="إجراءات سريعة" 
                                             FontSize="20" FontWeight="Bold" 
                                             Foreground="{StaticResource DarkText}"
                                             Margin="0,0,0,20"/>
                                    
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <Button Grid.Column="0" 
                                               Content="📋 إدارة المرضى"
                                               Style="{StaticResource NavigationButton}"
                                               Background="{StaticResource AccentGreen}"
                                               Command="{Binding NavigateCommand}"
                                               CommandParameter="PatientManagement"
                                               Margin="10"
                                               Padding="20,15"
                                               FontSize="16"
                                               Visibility="{Binding CanAddPatients, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        
                                        <Button Grid.Column="1" 
                                               Content="🧪 إدخال النتائج"
                                               Style="{StaticResource NavigationButton}"
                                               Background="{StaticResource AccentOrange}"
                                               Command="{Binding NavigateCommand}"
                                               CommandParameter="ResultEntry"
                                               Margin="10"
                                               Padding="20,15"
                                               FontSize="16"
                                               Visibility="{Binding CanManageTests, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        
                                        <Button Grid.Column="2" 
                                               Content="📊 معاينة النتائج"
                                               Style="{StaticResource NavigationButton}"
                                               Background="{StaticResource PrimaryDark}"
                                               Command="{Binding NavigateCommand}"
                                               CommandParameter="ReportView"
                                               Margin="10"
                                               Padding="20,15"
                                               FontSize="16"
                                               Visibility="{Binding CanManageTests, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </Grid>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource MediumGray}" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="الحالة: " Foreground="White" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="White"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="قاعدة البيانات: " Foreground="White" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding DatabaseStatus}" Foreground="White"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="التاريخ والوقت: " Foreground="White" FontWeight="SemiBold"/>
                    <TextBlock Name="StatusTimeLabel" Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
