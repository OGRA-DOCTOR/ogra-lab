<Window x:Class="OGRALAB.Views.TestTypesManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OGRALAB.Helpers"
        Title="إدارة أنواع التحاليل - OGRA LAB"
        Height="800" Width="1200"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        FontFamily="Cairo, Segoe UI">
    
    <Window.Resources>
        <!-- Color Resources -->
        <SolidColorBrush x:Key="PrimaryDark" Color="#1e3a8a"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#f59e0b"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#10b981"/>
        <SolidColorBrush x:Key="LightGray" Color="#f8fafc"/>
        <SolidColorBrush x:Key="MediumGray" Color="#64748b"/>
        <SolidColorBrush x:Key="DarkText" Color="#1e293b"/>
        <SolidColorBrush x:Key="CardBackground" Color="White"/>
        <SolidColorBrush x:Key="DangerRed" Color="#dc2626"/>

        <!-- Button Styles -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentOrange}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#d97706"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#b45309"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#9ca3af"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#047857"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource DangerRed}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#b91c1c"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#991b1b"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#e2e8f0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentOrange}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource LightGray}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryDark}" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Ellipse Width="40" Height="40" Fill="{StaticResource AccentOrange}" Margin="0,0,15,0"/>
                    <StackPanel>
                        <TextBlock Text="إدارة أنواع التحاليل" FontSize="22" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="إضافة وتعديل وحذف أنواع التحاليل الطبية" FontSize="14" Foreground="#cbd5e1"/>
                    </StackPanel>
                </StackPanel>
                
                <Button Grid.Column="1" 
                        Content="✕ إغلاق" 
                        Style="{StaticResource DangerButton}"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Test Types List -->
            <Border Grid.Column="0" Background="{StaticResource CardBackground}" CornerRadius="8" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="أنواع التحاليل" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkText}"/>
                        <Border Background="{StaticResource AccentGreen}" CornerRadius="12" 
                               Padding="6,2" Margin="10,0,0,0">
                            <TextBlock Text="{Binding TestTypesCount}" FontSize="12" 
                                     FontWeight="SemiBold" Foreground="White"/>
                        </Border>
                    </StackPanel>

                    <!-- Search -->
                    <Grid Grid.Row="1" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox Grid.Column="0" 
                               Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                               Style="{StaticResource ModernTextBox}"
                               Tag="البحث في أنواع التحاليل..."/>
                        
                        <Button Grid.Column="1" 
                               Content="🔍 بحث"
                               Style="{StaticResource SecondaryButton}"
                               Command="{Binding SearchCommand}"/>
                        
                        <Button Grid.Column="2" 
                               Content="🔄 تحديث"
                               Style="{StaticResource PrimaryButton}"
                               Command="{Binding RefreshCommand}"/>
                    </Grid>

                    <!-- Test Types DataGrid -->
                    <DataGrid Grid.Row="2" 
                            ItemsSource="{Binding TestTypes}"
                            SelectedItem="{Binding SelectedTestType}"
                            AutoGenerateColumns="False"
                            IsReadOnly="True"
                            GridLinesVisibility="Horizontal"
                            HeadersVisibility="Column"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            SelectionMode="Single"
                            FontSize="12">
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="اسم التحليل" Binding="{Binding TestName}" Width="*"/>
                            <DataGridTextColumn Header="الكود" Binding="{Binding TestCode}" Width="80"/>
                            <DataGridTextColumn Header="الوحدة" Binding="{Binding Unit}" Width="80"/>
                            <DataGridTextColumn Header="الفئة" Binding="{Binding Category}" Width="100"/>
                            <DataGridTextColumn Header="النطاق الطبيعي" Binding="{Binding NormalRange}" Width="120"/>
                            <DataGridCheckBoxColumn Header="نشط" Binding="{Binding IsActive}" Width="50"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0">
                        <Button Content="➕ إضافة" 
                               Style="{StaticResource SecondaryButton}"
                               Command="{Binding AddTestTypeCommand}"/>
                        
                        <Button Content="✏️ تعديل" 
                               Style="{StaticResource PrimaryButton}"
                               Command="{Binding EditTestTypeCommand}"/>
                        
                        <Button Content="🗑️ حذف" 
                               Style="{StaticResource DangerButton}"
                               Command="{Binding DeleteTestTypeCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Right Panel - Edit Form -->
            <Border Grid.Column="2" Background="{StaticResource CardBackground}" CornerRadius="8" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>
                
                <Grid Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Form Header -->
                    <TextBlock Grid.Row="0" Text="{Binding FormTitle}" FontSize="18" FontWeight="Bold" 
                             Foreground="{StaticResource DarkText}" Margin="0,0,0,20"/>

                    <!-- Form Fields -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- Test Name -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="اسم التحليل *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding TestName}" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>

                            <!-- Test Code -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="كود التحليل *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding TestCode}" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>

                            <!-- Unit -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="وحدة القياس" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding Unit}" Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>

                            <!-- Category -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="الفئة" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <ComboBox ItemsSource="{Binding Categories}"
                                        SelectedItem="{Binding Category}"
                                        IsEditable="True"
                                        FontSize="14" Padding="8,6" BorderThickness="2" BorderBrush="#e2e8f0"/>
                            </StackPanel>

                            <!-- Normal Range -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="النطاق الطبيعي" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBox Grid.Column="0" 
                                           Text="{Binding MinNormalValue}"
                                           Style="{StaticResource ModernTextBox}"
                                           Tag="الحد الأدنى"/>
                                    
                                    <TextBlock Grid.Column="1" Text=" - " VerticalAlignment="Center" 
                                             HorizontalAlignment="Center" Margin="5,0"/>
                                    
                                    <TextBox Grid.Column="2" 
                                           Text="{Binding MaxNormalValue}"
                                           Style="{StaticResource ModernTextBox}"
                                           Tag="الحد الأعلى"/>
                                </Grid>
                                
                                <TextBox Text="{Binding NormalRange}" 
                                       Style="{StaticResource ModernTextBox}"
                                       Margin="0,5,0,0" IsReadOnly="True"
                                       Background="#f8fafc"/>
                            </StackPanel>

                            <!-- Description -->
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="الوصف" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding Description}" 
                                       Style="{StaticResource ModernTextBox}"
                                       AcceptsReturn="True" 
                                       TextWrapping="Wrap" 
                                       Height="80"/>
                            </StackPanel>

                            <!-- Is Active -->
                            <CheckBox Content="نشط" IsChecked="{Binding IsActive}" 
                                    FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Form Buttons -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <Button Content="💾 حفظ" 
                               Style="{StaticResource SecondaryButton}"
                               Command="{Binding SaveTestTypeCommand}"
                               Padding="20,10"/>
                        
                        <Button Content="❌ إلغاء" 
                               Style="{StaticResource DangerButton}"
                               Command="{Binding CancelEditCommand}"
                               Padding="15,10"/>
                    </StackPanel>
                </Grid>

                <!-- No Edit Mode Message -->
                <Border Background="#f0f9ff" CornerRadius="6" Padding="20" 
                       Visibility="{Binding IsEditMode, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🧪" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="اختر نوع تحليل للتعديل" FontSize="16" FontWeight="SemiBold" 
                                 HorizontalAlignment="Center" Foreground="{StaticResource DarkText}"/>
                        <TextBlock Text="أو انقر على 'إضافة' لإنشاء نوع جديد" 
                                 FontSize="14" HorizontalAlignment="Center" 
                                 Foreground="{StaticResource MediumGray}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>

        <!-- Error Message -->
        <Border Grid.Row="2" Background="#fee2e2" CornerRadius="6" Padding="15" 
                Margin="20,0,20,10"
                Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠️" FontSize="16" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#dc2626" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" 
             Background="#80000000" 
             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                           Width="200" 
                           Height="4" 
                           Background="Transparent" 
                           Foreground="{StaticResource AccentOrange}"/>
                <TextBlock Text="جاري المعالجة..." 
                         Foreground="White" 
                         FontSize="16" 
                         Margin="0,15,0,0"
                         HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
