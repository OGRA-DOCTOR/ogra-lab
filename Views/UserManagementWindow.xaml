<Window x:Class="OGRALAB.Views.UserManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OGRALAB.Helpers"
        Title="إدارة المستخدمين - OGRA LAB" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        FontFamily="Segoe UI">
    
    <Window.Resources>
        <!-- Color Resources -->
        <SolidColorBrush x:Key="PrimaryDark" Color="#1e3a8a"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#f59e0b"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#10b981"/>
        <SolidColorBrush x:Key="LightGray" Color="#f8fafc"/>
        <SolidColorBrush x:Key="MediumGray" Color="#64748b"/>
        <SolidColorBrush x:Key="DarkText" Color="#1e293b"/>

        <!-- Button Styles -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentOrange}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#d97706"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#b45309"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#9ca3af"/>
                                <Setter Property="Foreground" Value="#6b7280"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#047857"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#dc2626"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#b91c1c"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#991b1b"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#e2e8f0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentOrange}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#e2e8f0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentOrange}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#e2e8f0"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource LightGray}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryDark}" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Ellipse Width="40" Height="40" Fill="{StaticResource AccentOrange}" Margin="0,0,15,0"/>
                    <StackPanel>
                        <TextBlock Text="إدارة المستخدمين" FontSize="22" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="إضافة وتعديل وحذف مستخدمي النظام" FontSize="14" Foreground="#cbd5e1"/>
                    </StackPanel>
                </StackPanel>
                
                <Button Grid.Column="1" 
                        Content="✕ إغلاق" 
                        Style="{StaticResource DangerButton}"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Users List Section -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Search Section -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="قائمة المستخدمين" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkText}" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Grid.Column="0" 
                                   Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                   Style="{StaticResource ModernTextBox}"
                                   Tag="البحث بالاسم أو اسم المستخدم أو البريد الإلكتروني"/>
                            
                            <Button Grid.Column="1" 
                                   Content="🔍 بحث" 
                                   Style="{StaticResource SecondaryButton}"
                                   Command="{Binding SearchCommand}"/>
                                   
                            <Button Grid.Column="2" 
                                   Content="🔄 تحديث" 
                                   Style="{StaticResource PrimaryButton}"
                                   Command="{Binding LoadUsersCommand}"/>
                        </Grid>
                    </StackPanel>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="➕ إضافة مستخدم" 
                               Style="{StaticResource PrimaryButton}"
                               Command="{Binding AddUserCommand}"/>
                        <Button Content="✏️ تعديل" 
                               Style="{StaticResource SecondaryButton}"
                               Command="{Binding EditUserCommand}"/>
                        <Button Content="🗑️ حذف" 
                               Style="{StaticResource DangerButton}"
                               Command="{Binding DeleteUserCommand}"/>
                        <Button Content="🔐 إعادة تعيين كلمة المرور" 
                               Style="{StaticResource SecondaryButton}"
                               Command="{Binding ResetPasswordCommand}"/>
                    </StackPanel>

                    <!-- Users DataGrid -->
                    <DataGrid Grid.Row="2" 
                             ItemsSource="{Binding Users}"
                             SelectedItem="{Binding SelectedUser}"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             GridLinesVisibility="Horizontal"
                             HeadersVisibility="Column"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False"
                             CanUserReorderColumns="True"
                             CanUserResizeColumns="True"
                             CanUserSortColumns="True"
                             AlternatingRowBackground="#f8fafc"
                             SelectionMode="Single"
                             FontSize="12">
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="اسم المستخدم" Binding="{Binding Username}" Width="120"/>
                            <DataGridTextColumn Header="الاسم الكامل" Binding="{Binding FullName}" Width="*"/>
                            <DataGridTextColumn Header="البريد الإلكتروني" Binding="{Binding Email}" Width="200"/>
                            <DataGridTextColumn Header="الدور" Binding="{Binding Role}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Role}" Value="SystemUser">
                                                <Setter Property="Foreground" Value="#dc2626"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="AdminUser">
                                                <Setter Property="Foreground" Value="#f59e0b"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="RegularUser">
                                                <Setter Property="Foreground" Value="#10b981"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="الحالة" Width="80">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="IsActive">
                                        <Binding.Converter>
                                            <local:BoolToStringConverter TrueValue="نشط" FalseValue="معطل"/>
                                        </Binding.Converter>
                                    </Binding>
                                </DataGridTextColumn.Binding>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                <Setter Property="Foreground" Value="#10b981"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                <Setter Property="Foreground" Value="#dc2626"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="آخر دخول" Binding="{Binding LastLoginDate, StringFormat=dd/MM/yyyy HH:mm}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Status Info -->
                    <Border Grid.Row="3" Background="{StaticResource LightGray}" Padding="10" CornerRadius="4" Margin="0,15,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="إجمالي المستخدمين: " FontWeight="SemiBold" Foreground="{StaticResource DarkText}"/>
                            <TextBlock Text="{Binding Users.Count}" FontWeight="Bold" Foreground="{StaticResource AccentOrange}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- User Form Section -->
            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="20"
                    Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                </Border.Effect>
                
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="{Binding SelectedUser, Converter={StaticResource UserFormTitleConverter}}" 
                                 FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkText}" Margin="0,0,0,20"/>

                        <!-- Username -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="اسم المستخدم *" FontWeight="SemiBold" Foreground="{StaticResource DarkText}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                                   Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>

                        <!-- Full Name -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="الاسم الكامل *" FontWeight="SemiBold" Foreground="{StaticResource DarkText}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" 
                                   Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>

                        <!-- Email -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="البريد الإلكتروني *" FontWeight="SemiBold" Foreground="{StaticResource DarkText}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                                   Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>

                        <!-- Phone Number -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="رقم الهاتف" FontWeight="SemiBold" Foreground="{StaticResource DarkText}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" 
                                   Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>

                        <!-- Role -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="الدور *" FontWeight="SemiBold" Foreground="{StaticResource DarkText}" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding AvailableRoles}" 
                                    SelectedItem="{Binding SelectedRole}"
                                    Style="{StaticResource ModernComboBox}"/>
                        </StackPanel>

                        <!-- Password -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock FontWeight="SemiBold" Foreground="{StaticResource DarkText}" Margin="0,0,0,5">
                                <TextBlock.Text>
                                    <Binding Path="SelectedUser" Converter="{StaticResource PasswordLabelConverter}"/>
                                </TextBlock.Text>
                            </TextBlock>
                            <PasswordBox Name="PasswordBox" 
                                       Style="{StaticResource ModernPasswordBox}"
                                       PasswordChanged="PasswordBox_PasswordChanged"/>
                        </StackPanel>

                        <!-- Confirm Password -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="تأكيد كلمة المرور" FontWeight="SemiBold" Foreground="{StaticResource DarkText}" Margin="0,0,0,5"/>
                            <PasswordBox Name="ConfirmPasswordBox" 
                                       Style="{StaticResource ModernPasswordBox}"
                                       PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>
                        </StackPanel>

                        <!-- Is Active -->
                        <CheckBox Content="المستخدم نشط" 
                                IsChecked="{Binding IsActive}"
                                FontWeight="SemiBold"
                                Foreground="{StaticResource DarkText}"
                                Margin="0,0,0,20"/>

                        <!-- Error Message -->
                        <TextBlock Text="{Binding ErrorMessage}"
                                 Foreground="#dc2626"
                                 FontWeight="SemiBold"
                                 TextWrapping="Wrap"
                                 Margin="0,0,0,15"
                                 Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

                        <!-- Action Buttons -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="💾 حفظ" 
                                   Style="{StaticResource PrimaryButton}"
                                   Command="{Binding SaveUserCommand}"/>
                            <Button Content="❌ إلغاء" 
                                   Style="{StaticResource DangerButton}"
                                   Command="{Binding CancelEditCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Loading Overlay -->
            <Grid Grid.Column="0" Grid.ColumnSpan="3" 
                 Background="#80000000" 
                 Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" 
                               Width="200" 
                               Height="4" 
                               Background="Transparent" 
                               Foreground="{StaticResource AccentOrange}"/>
                    <TextBlock Text="جاري المعالجة..." 
                             Foreground="White" 
                             FontSize="16" 
                             Margin="0,15,0,0"
                             HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
