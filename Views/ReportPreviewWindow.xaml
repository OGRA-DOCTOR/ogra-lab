<Window x:Class="OGRALAB.Views.ReportPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OGRALAB.Helpers"
        Title="معاينة التقرير - OGRA LAB"
        Height="800" Width="1000"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        FontFamily="Cairo, Segoe UI">
    
    <Window.Resources>
        <!-- Color Resources -->
        <SolidColorBrush x:Key="PrimaryDark" Color="#1e3a8a"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#f59e0b"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#10b981"/>
        <SolidColorBrush x:Key="LightGray" Color="#f8fafc"/>
        <SolidColorBrush x:Key="DarkText" Color="#1e293b"/>
        <SolidColorBrush x:Key="CardBackground" Color="White"/>

        <!-- Button Styles -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentOrange}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#d97706"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#b45309"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#9ca3af"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#047857"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource LightGray}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryDark}" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Ellipse Width="40" Height="40" Fill="{StaticResource AccentOrange}" Margin="0,0,15,0"/>
                    <StackPanel>
                        <TextBlock x:Name="HeaderTitle" Text="معاينة تقرير نتائج المريض" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="HeaderSubtitle" Text="مراجعة التقرير قبل الطباعة أو الحفظ" FontSize="14" Foreground="#cbd5e1"/>
                    </StackPanel>
                </StackPanel>
                
                <Button Grid.Column="1" 
                        Content="✕ إغلاق" 
                        Background="#dc2626"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="12,6"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <!-- Report Preview -->
        <Border Grid.Row="1" Background="{StaticResource CardBackground}" Margin="20" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
            </Border.Effect>
            
            <ScrollViewer x:Name="ReportScrollViewer" 
                         VerticalScrollBarVisibility="Auto" 
                         HorizontalScrollBarVisibility="Auto"
                         Padding="20">
                
                <!-- This will contain the generated report -->
                <FlowDocumentScrollViewer x:Name="ReportViewer" 
                                        Document="{Binding ReportDocument}"
                                        Zoom="100"
                                        IsSelectionEnabled="True"
                                        IsToolBarVisible="False"
                                        Background="White"/>
            </ScrollViewer>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="2" Background="{StaticResource CardBackground}" Padding="20" BorderBrush="#e2e8f0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="PrintButton"
                        Content="🖨️ طباعة التقرير" 
                        Style="{StaticResource SecondaryButton}"
                        Click="PrintButton_Click"
                        Padding="20,10"/>
                
                <Button Content="💾 حفظ كـ PDF" 
                        Style="{StaticResource PrimaryButton}"
                        Click="SaveAsPdfButton_Click"
                        Padding="20,10"/>
                
                <Button Content="📋 نسخ للحافظة" 
                        Style="{StaticResource PrimaryButton}"
                        Background="#6366f1"
                        Click="CopyToClipboardButton_Click"
                        Padding="15,10"/>
                
                <Button Content="🔍 تكبير" 
                        Style="{StaticResource PrimaryButton}"
                        Background="#8b5cf6"
                        Click="ZoomInButton_Click"
                        Padding="15,10"/>
                
                <Button Content="🔍 تصغير" 
                        Style="{StaticResource PrimaryButton}"
                        Background="#8b5cf6"
                        Click="ZoomOutButton_Click"
                        Padding="15,10"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
